# Final render staging

Optional staging directory for candidate final renders before replacing canonical assets in `paper/figures/`.

Copy validated outputs into canonical filenames (`figureN.png`, `hero.png`) once synchronization checks pass.
